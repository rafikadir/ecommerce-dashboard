import React from 'react';
import { Container } from 'react-bootstrap';

const Products = () => {
    return (
        <Container fluid>
            <h1>Product page</h1>
        </Container>
    );
};

export default Products;